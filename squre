#include "newfunc.h"
#include <math.h>

int squre(, , ) {
    return fst_x;
}

int squres(int a, int b, int c) {
    int fst_x = 0;
    int snd_x = 0;
    int d = -1;
    flag = 0;

    if (a == 0 && b == 0)
        return 0;

    if (a == 0)
        flag += 1;
    if (b == 0)
        flag += 2;
    if (c == 0)
        flag +=4;

/*    switch(flag){
    case 0:
        d = (b^2) + (2*a*c)
        if (d < 0)
            return 0;

        break;
    case 1:

        break;
    case 2:

        break;
    case 3:
            break;
    case 5:

            break;
    case 6:
            return sqrt(-a,2);
            break;
    case 7:
            return 0;
            break;
    }
*/
    int i = 2;
    while (i < num) {
        next += prev;
        prev = next - prev;
        i++;
    }

    return next;
}
